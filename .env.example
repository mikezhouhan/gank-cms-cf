PAYLOAD_SECRET=THISISYOURSECRET
NEXT_PUBLIC_URL=http://localhost:3600

# PostgreSQL Database URL (for local development)
# In production, this will be provided by Cloudflare Hyperdrive
# Use sslmode=no-verify to skip certificate hostname validation when connecting via IP
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=no-verify
